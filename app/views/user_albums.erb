    <div id="main">
        <div class="header">
            <h1><%= @user.username %></h1>
            <h2></h2>
        </div>

        <div class="content">
            <h2 class="content-subhead">Albums:</h2>
            <ul>
                <% @user.albums.each do |album| %>
                <li><a href="/albums/<%= album.id %>"><%= album.name %></a></li>
                <% end %>
        </div>
    </div>
</div>
